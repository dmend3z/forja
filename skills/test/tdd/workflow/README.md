# TDD Workflow

Test-Driven Development skill for Claude Code.

## Usage

```
forja install test/tdd/workflow
```

Then use `/tdd` in Claude Code to invoke the TDD cycle.

## What it provides

- **Agent**: `tdd-guide` — enforces write-tests-first methodology
- **Skill**: `tdd-workflow` — slash command for the full TDD cycle
